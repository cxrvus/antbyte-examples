set start = top_left;
set fps = 4;
set midi_out_ch = 5;

ant 0 {
	(X3, X2, X1, X0) = buf(R3, R2, R1, R0);
	(C3, C2, C1, C0) = buf(X3, X2, X1, X0);
	W = R4;
	A = R5;
}

ant 1 {
	(X3, X2, X1, X0) = buf(C3, C2, C1, C0);
	W1 = J;
	Z = -J;
}
