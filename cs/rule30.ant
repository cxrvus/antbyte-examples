set { size = 0x80; speed = 0x400; }
set { start = top; bg_filter = one; }

ant 0 {
	(l, c, r) = buf(VC50, VC60, VC70);
	(x0, x1, x2, x3, x4, x5, x6, x7) = dec(l, c, r);
	C = or(x1, x2, x3, x4);
	C = or(J, C);

	C1 = J; Z = VC01;
}
