set desc = "randomly wanders in straight lines, generating interesting patterns!";
set speed = 32;
set fps = 16;
set loop;
set sleep = 50;
set border = die;
set bg_filter = bin;

ant main = 1 {
	(D2, D1) = fwd(R2, R1);
	C0 = 1;
}
