set size = 19;
set speed = 361;
set fps = 12;

ant main = 1 {
	paint = and(M1, M0);

	(M1, M0) = fwd(
		and(-paint, M1),
		and(-paint, M0)
	);

	(cout, M0) = add(M0, T0);
	(_, M1) = add(M1, 0, cout);

	(C2, C1) = fwd(M1, M0);
}
