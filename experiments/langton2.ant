use "../cs/langton";

set desc = "Langton's Ant, but with R2 instead of R1";

set size = 128;
set border = cycle;
set speed = 32;

fn main = in_color => out_color {
	out_color = langton(in_color);
	R2 = R1;
	R1 = 0;
}

ant main = 1 {
	T0 = main(T0);
}
