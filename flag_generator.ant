set start = top_left;

set width = 16;
set height = 12;
set speed = 16;

ant main {
	M0 = mux(TT5, M0, R0);
	M1 = mux(TT5, M1, R1);
	M2 = mux(TT5, M2, R2);

	(C2, C1, C0) = buf(M2, M1, M0);

	Z = VC3;
	C3 = 1;
}
