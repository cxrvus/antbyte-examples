set desc = "randomly wanders in straight lines, generating interesting patterns!";
set speed = 32;
set fps = 16;
set loop;
set sleep = 50;
set border = die;
set bg_filter = bin;

ant main = 1 {
	(R0, R1) = fwd(N2, N1);
	T0 = 1;
}
