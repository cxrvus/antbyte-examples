set start = top_left;

set width = 16;
set height = 12;
set speed = 16;

ant main = 1 {
	M0 = mux(C15, M0, N0);
	M1 = mux(C15, M1, N1);
	M2 = mux(C15, M2, N2);

	(T2, T1, T0) = fwd(M2, M1, M0);

	Z = VT3;
	T3 = 1;
}
